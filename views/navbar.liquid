{% block content %}
<nav class="navbar navbar-expand-md navbar-dark bg-dark">
    <div class="container-fluid">
        <a href="/logs" class="navbar-brand">
        <img src="../imgs/movieloggericon.svg"width="80"height="60" class="d-inline-block align-top">
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            {% if loggedIn %}
                <a href="/logs" class="nav-link text-light">View All</a>
                <a href="/logs/new" class="nav-link text-light">New Log</a>
                <a href="/logs/mine" class="nav-link text-light">My Logs</a>
                <a href="/auth/logout" class="nav-link text-light">Sign Out</a>
            {% else %}
                <a href="/auth/signup" class="nav-link text-light">Sign Up</a>
                <a href="/auth/login" class="nav-link text-light">Sign In</a>
            {% endif %}
        </div>
    </div>
</nav>
{% endblock %}